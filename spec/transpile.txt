Translation scheme:

typedef vx_object (*vx_call)(vx_object*, int);

typedef struct vx_object vx_object;
typedef enum vx_obj_type {
	VX_CONST,
	VX_STR,
	VX_FUNC
} vx_obj_type;

struct vx_function {
	enum vx_func_type { VX_BUILTIN, VX_DEFINED };

	
}

struct vx_object {
	vx_obj_type type;
	
	union {
		double o_const;
		string o_str;
		vx_func o_func;
	}	
};

unordered_map<string, 